{
  "tests": [
    {
      "name": "HTML5 DOCTYPE",
      "run": "grep -q \"<!DOCTYPE html>\" index.html",
      "points": 20
    },
    {
      "name": "Semantic Elements",
      "run": "grep -q \"<header\" index.html && grep -q \"<nav\" index.html && grep -q \"<main\" index.html",
      "points": 30
    }
  ]
}
